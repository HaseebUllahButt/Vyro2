# ── Training (run in 01_train.ipynb on T4 GPU) ─────────────────────────────
torch>=2.2.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.12.0
datasets>=2.19.0
bitsandbytes>=0.43.0
accelerate>=0.29.0
huggingface_hub>=0.20.0

# ── Inference + Demo (run in 02_judge_demo.ipynb on CPU) ──────────────────
llama-cpp-python>=0.2.57
gradio>=4.31.0

# ── NOTE ──────────────────────────────────────────────────────────────────
# Python standard library modules are NOT listed here.
# They ship with Python and pip install will fail if you add them.
# Examples used in this project: re, json, os, datetime, pathlib, typing, hashlib
# The sequence-matching module (used in inference.py) is also stdlib — do not pip install it.
